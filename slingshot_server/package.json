{
  "name": "slingshot_server",
  "version": "1.2.1",
  "description": "This server runs a trie database which clients can interact with using a CLI",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Arnav",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
